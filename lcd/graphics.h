#include "lcd.h"

void grInit(orientation orn, colour fg = WHITE, colour bg = BLACK);
void grDrawBox(coord pos, uint16_t width, uint16_t height, uint8_t lineWidth, colour fg = WHITE, colour bg = BLACK);